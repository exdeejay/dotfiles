#!/bin/bash

if [[ $# != 1 ]]; then
	echo 'Usage: makereal <symlink>'
	return 1
fi
if [[ ! -h "$1" ]]; then
	echo "Error: $1 is not a symlink"
	return 1
fi
REAL="$(readlink -f "$1")"
rm "$1"
cp -r "$REAL" "$1"
echo "$1"

